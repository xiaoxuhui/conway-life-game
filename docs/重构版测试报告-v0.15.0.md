# 重构版测试报告 v0.15.0

## 环境

- 日期：2026-08-27
- 系统：Windows
- 运行时：Codex 随附 Node.js v24.19.0
- 分支：`refactor/conway-full`

## 自动化测试

- 完整回归：91 项通过，0 项失败，0 项跳过；总耗时约 65.9 秒。
- 新增覆盖：共享 `nextCellSet` 与世界演化一致且不修改输入；四个逻辑子模块与兼容门面一致；相位预演同步推进输出探针、终端和连接探针。
- 重型物理验证：双分支 XOR、多层门、15 门保存函数、安全时域搜索均通过。
- JavaScript 语法：`scripts/*.js` 全部通过 `node --check`。
- 差异质量：`git diff --check` 通过。
- ESLint：当前机器没有可用的本地 ESLint/npm 入口；遵守离线约束，未联网下载，因此本轮未执行。

## 兼容性

- 保留传统 `<script defer>` 加载方式，没有引入 ES Module 或打包器。
- `index.html` 的依赖顺序为 LifeEngine → parse → expand/compile/safety → LogicCode 门面。
- Node CommonJS 与浏览器全局 UMD 两种入口均由语法、模块引用和页面静态测试覆盖。
- JSON 存档、图案库和函数库存储格式没有变化。

## 真实浏览器检查

- 尝试接管已打开的 `file:///D:/soft/conway-life-game/index.html`。
- Codex 浏览器安全策略拒绝控制 `file://` 页面，并明确禁止换用绕行方式；因此未声称完成真实交互或桌面/移动视觉验收。
- 待人工核验：页面无控制台错误；开始/暂停与单步；经典图案放置；导入导出；输入 `NOT(AND(0,0))` 生成逻辑结构；窄屏无横向溢出。

## 结论

v0.15.0 的本地代码重构和自动化验证通过。浏览器交互、ESLint、标签、合并与推送仍是发布前门禁。
