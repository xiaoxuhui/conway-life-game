# 测试报告

## 环境

- 日期：2026-08-22
- 系统：Windows
- 自动化测试：Codex 随附 Node.js 运行时，Node 内置测试框架
- 交互测试：本轮浏览器控制连接故障，未执行新的真实指针回归

## 自动化测试

- 结果：88 项通过，0 项失败，0 项跳过。
- JavaScript 语法检查：`life-engine.js`、`presets.js`、`pattern-library.js`、`speed-control.js`、`logic-code.js`、`logic-function-library.js`、`renderer.js` 和 `app.js` 全部通过。
- 生命规则覆盖：空世界、负坐标静物、百万格外振荡器、跨旧边界滑翔机、输入不可变、随机世界、负坐标图案和包围盒。
- 内置图案覆盖：24 种图案的坐标范围和唯一性；方块、蜂巢、面包、小船的稳定性；蟾蜍和信标的二周期；顽强者第 130 代消失；高斯帕滑翔机枪第 30 代发射滑翔机。
- 相机覆盖：屏幕/世界坐标映射、负坐标拾取、平移方向、适应图案、缩放上下限和光标锚定缩放。
- 自定义图案覆盖：负坐标归一化、空世界、名称/说明限制、重名、更新、删除、本地保存与恢复、损坏数据、写入失败、非法坐标和 200 项上限。
- 数据与页面覆盖：JSON v1/v2 存档、核心控件、本地资源、对话框字段与可访问反馈。
- v0.5 放置覆盖：多次追加不会替换已有世界；重叠细胞去重；非正方形旋转尺寸与坐标；四次旋转、两次翻转复原；放置按钮和快捷键可访问标记。
- v0.6.2 逻辑门覆盖：NOT 的 2 行、AND 的 4 行、OR 的 4 行完整真值表均由完整五细胞滑翔机验证；十个静物输出端连续 600 代保持隔离且不变，真输出滑翔机四代后按正确方向继续移动；新增回归覆盖 AND `0∧0` 早期单格误亮、AND `0∧1` 交换律、几何变换和锁存标签。
- v0.6.3 碰撞几何覆盖：AND、OR 的两个输入原点 Y 坐标恰好相差 1、X 坐标相隔 41（奇数）；新布局的完整 8 行双输入真值、滑翔机传播、端点 600 代稳定性及输入原点旋转/翻转均通过。
- v0.6.4 偶数间隔覆盖：AND、OR 的两个输入原点 Y 坐标继续相差 1，X 坐标改为相隔 42（偶数）；AND 第 150 代与 OR 第 243 代的完整真值、传播及端点 600 代稳定性均通过。
- v0.7.0 速度输入覆盖：页面不再包含速度滑块，数字输入框公开 1–20 范围和单位；有效整数、编辑空值、非数字、小数取整和上下限裁剪均由纯函数测试覆盖。
- v0.8.0 逻辑代码覆盖：NOT、AND、OR 的英文、中文、符号和函数写法；空值、未知运算、非二进制值和错误参数数量；全部 10 种输入到现有预设 ID、输入元数据和期望真值的一致映射。
- v0.8.1 嵌套覆盖：`NOT(AND(0,0))` 展开顺序、两级组合结构、最终完整滑翔机 `O=1`、多分支后序步骤和超过 8 层时的受控错误。
- v0.8.2 真实级联覆盖：可连接门体能无损还原全部十种旧门；AND→NOT 的空信号与真滑翔机分别得到 1/0；两条连续线路逐级携带真值。
- v0.8.3 输入保留覆盖：成功生成嵌套结构后，应用层不得用内部格式化表达式覆盖逻辑代码输入框。
- v0.8.4 OR→NOT 稳定性覆盖：四种 OR 常量输入的连接真值和最终 NOT 输出；根 NOT 枪体在 0–900 代内每 30 代恢复完整 p30 结构。
- v0.8.5 OR 吞噬者覆盖：四种 OR 输入均演化 900 代；内部七细胞 Eater 1 每 30 代恢复完整，原真值、输出探针和静物终端保持正确。
- v0.9.0 变量与双分支覆盖：变量赋值成功路径及缺失、重复、非法、未使用错误；异或表达式的四组 A/B 真值；每组 4 条连接在对齐代的完整滑翔机真值和根 OR 最终物理输出。
- v0.10.0 我的函数覆盖：名称和 200 字代码限制、大小写无关重名、稳定 ID 与时间、修改、删除、本地存储往返、损坏 JSON、未来版本以及保存/载入/管理页面控件。
- v0.11.0 参数函数覆盖：无赋值主体和带默认赋值的输入识别、0/1 实例化、缺失/非法输入、v1→v2 存储迁移及动态输入控件页面结构。
- v0.12.0 嵌套函数覆盖：`NOT(XOR(1,0))` 单层调用、`SAME→XOR` 多层调用、错误参数数量、直接循环引用和应用层展开入口。
- v0.13.0 枪体安全覆盖：安全候选、零代外来侵入的失败关闭、所有嵌套真值测试的逐枪逐代验证，以及至少覆盖候选包围盒穿越时间的证明时域。
- v0.13.1 函数展开覆盖：调用名 `GF_2` 的数字与下划线、四个实参、307 字符内部展开的正常解析，以及超过 5000 字符时的受控错误。
- v0.13.2 混合语法覆盖：`GF_2` 展开后的括号内中缀运算、函数式与中缀式混用、`NOT > AND > OR` 优先级、保存时的输入识别，以及未知函数名的明确错误。
- v0.13.3 响应性覆盖：同步/异步结构完全一致、异步验证主动让出执行权、代数进度回调、取消信号、应用层异步入口、忙碌状态和取消文案。5 门异或基准由一次约 6.4 秒的连续阻塞改为约 8.5 秒总耗时内 447 次主动让出与 446 次进度更新，安全时域仍为 1688 代。
- v0.13.4 验证加速覆盖：默认时间片固定为 40ms；异步枪体占用区 p30 相位缓存仅在第 30 代与第 0 代一致时启用，否则逐代回退。相同 5 门基准约 5.7 秒、120 次主动让出和 119 次进度更新，仍验证到第 1688 代并生成相同 505 个细胞。
- v0.13.5 稀疏比较覆盖：枪体安全区改用预期活细胞与实际活细胞的双向包含比较；新增重叠安全区回归，确保同一坐标按枪组独立判断。5 门、4 线路基准仍验证 1688 代，本机约 3.9 秒完成。
- v0.14.0 高速演化覆盖：速度输入接受 1、60、300、1000 并拒绝越界值；时间累计覆盖低速小数代数、高速批量代数、250ms 帧间隔上限与 0.25 秒积压上限；页面静态检查锁定实际速度读数、`requestAnimationFrame`、10ms 预算，并拒绝重新引入 `setInterval`。
- v0.14.1 多布局覆盖：保存函数 XOR 展开到用户报告的 15 门表达式后，异步组合器优先使用布局 4、间距 240，生成 14 条真实连接并通过第 4236 代枪体安全验证；原固定布局在多档间距下失败的场景不再误报无安全线路。
- v0.14.2 通道布局覆盖：2 门链强制使用 40×p30 分支通道后仍生成安全结构，连接数量、完整验证时域和统一输出方向成立；15 门函数记录布局 4、20×p30 通道、余量 240 的只读路由元数据并继续通过 4236 代验证。

## 交互测试

- 放置滑翔机后，“保存为图案”显示 `3 × 3 · 5 个活细胞`预览。
- 保存“我的滑翔机”后，下拉列表出现“我的图案”项，当前世界仍保持 5 个活细胞。
- 刷新页面后自定义图案仍存在，重新放置后形状、说明和活细胞数正确。
- 输入同名图案时不会覆盖；页面显示重名原因，并仅在此时展示“更新同名图案”。
- 明确更新后说明立即变化，图案 ID 保持不变。
- 管理对话框可重命名；删除第一次点击进入确认状态，第二次点击才真正删除。
- 删除图案后当前世界仍保持 5 个活细胞，管理按钮在空图案库时禁用。
- 浏览器控制台：0 条错误，0 条警告。

## 兼容与视觉检查

- 桌面对话框：缩略预览、名称、说明、错误区与按钮完整显示，无重叠或裁切。
- 手机断点：测试视口对应约 390 × 844 CSS 设备规格；页面无横向溢出，对话框保持在视口内，次级图案按钮高度约 44px。
- 页面不引用远程 URL，不使用 ES Module，可继续直接双击 `index.html` 离线运行。

## 已知风险

- 大型嵌套的完整安全验证仍可能持续数秒或更久，但 v0.13.3 会保持页面响应、显示进度并允许取消；安全标准没有降低，85 项完整回归测试已覆盖。

- v0.12.0 的函数调用名列表仍需浏览器目视复验；嵌套展开、参数错误和循环引用自动测试已通过。

- v0.11.0 的参数输入控件仍需浏览器目视复验；定义、实例化和旧数据迁移自动测试已通过。

- v0.10.0 的函数保存、刷新载入和手机管理对话框仍需浏览器目视复验；数据往返和错误边界自动测试已通过。

- v0.9.0 的完整异或代码仍需在本地页面刷新后目视确认预览与拖动；变量错误边界、四组异或真值、逐线路滑翔机和最终探针自动测试已通过。

- v0.8.5 的 OR 内部吞噬过程仍需在本地页面刷新后目视观察；四种 OR 输入和 900 代吞噬者稳定性自动测试已通过。

- v0.5 的真实鼠标拖动、右键退出和窄屏布局尚未由自动浏览器复验；本轮连接组件无法启动。代码路径、静态结构、语法和 44 项自动测试均已通过，仍建议用户刷新本地页面做一次快速手动确认。

- 自动化浏览器安全策略禁止直接控制 `file://` 页面，因此文件模式由静态检查覆盖，交互验收使用内容相同的本地静态服务器。
- 浏览器按来源隔离本地存储，`file://` 与不同端口的本地服务器通常不会共享“我的图案”。
- 首版没有图案库文件导入/导出；清除浏览器网站数据会移除自定义图案。
- 首版保存当前世界的全部活细胞，不提供局部框选。
- 尚未执行 Firefox 与 Edge 的独立兼容性回归。
- v0.4 只改变内置图案数据，没有改变选择控件结构；自动化规则检查已通过，但本轮浏览器控制连接故障，因此 14 项长列表的桌面/窄屏点击检查待用户刷新页面确认。
