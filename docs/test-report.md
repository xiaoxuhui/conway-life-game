# 测试报告

## 环境

- 日期：2026-08-19
- 系统：Windows
- 自动化测试：Codex 随附 Node.js 运行时，Node 内置测试框架
- 交互测试：Codex 应用内 Chromium 浏览器，本地静态服务器
- 测试地址：`http://127.0.0.1:43119/`（仅验收期间使用）

## 自动化测试

- 结果：14 项通过，0 项失败，0 项跳过。
- JavaScript 语法检查：4 个脚本全部通过。
- 覆盖：空棋盘、单细胞、方块静物、闪烁器、滑翔机、有限边界、输入不可变、确定性随机、JSON 往返、非法数据、越界图案、预设完整性、核心控件和离线资源结构。

## 交互测试

- 点击一个格子后活细胞数从 0 变为 1。
- 拖动连续绘制后活细胞数变为 3。
- 清空后活细胞数归零，撤销后恢复原局面。
- 载入滑翔机后为 5 个活细胞；单步只增加一代。
- 开始后代数连续增加、单步禁用；暂停后代数停止。
- 运行中点击棋盘不会编辑，并显示“请先暂停”。
- 调速到 20 代/秒后标签和运行速度更新。
- 棋盘获得焦点后，空格、右方向键和 C 快捷键通过。
- 合法 JSON 导入恢复第 17 代和 4 个活细胞。
- 非法 JSON 显示错误，代数和局面保持不变。
- 导出控件执行并显示成功反馈；浏览器的自动下载事件未被测试接口捕获。
- 刷新页面后回到第 0 代、暂停状态。

## 兼容与视觉检查

- 桌面测试目标：1280 × 900；控制区为四列布局，Canvas 完整显示。
- 手机测试目标：390 × 844；无页面级横向溢出，控件无重叠或越界，最小控件高度约 48px，Canvas 完整显示。
- 浏览器控制台：0 条错误，0 条警告。
- 页面不引用远程 URL，不使用 ES Module。

## 已知风险

- 自动化浏览器的安全策略禁止导航到 `file://`，所以双击 `index.html` 需要用户人工确认；静态检查已确认资源均为相对本地文件。
- 尚未在真实触摸屏上验证拖动手势。
- 尚未执行 Firefox 与 Edge 的独立兼容性回归。
- 下载事件未被测试接口捕获，但页面成功执行导出流程且无控制台错误。
