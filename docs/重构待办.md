# conway-life-game 重构待办

## 目标

在独立分支 `refactor/conway-full` 上，按 `plan-driven-frontend-dev` skill 推进**全量重构**：
拆巨石文件（`app.js` / `logic-code.js`）、修安全隐患、补工程化缺口。
**保持零依赖、双击 `index.html` 离线可用**这一核心卖点不变。

## 执行原则

- 每子任务一个 commit，前缀严格用 `refactor:` / `fix:` / `chore:` / `ci:`。
- 每次重构后跑 `node --test` 验证，88 项用例须仍全过。
- **不引入打包器 / ES module**，保留 UMD 双模式（`window.*` 全局 + `module.exports`）与 `file://` 离线特性。
- 合并前做真实浏览器交互验证（打开 `index.html` 无 JS 错误）。

## P0：安全与最大维护债

- [x] `fix:` `expandFunctions` 改用函数式替换消除 `$` 注入（`logic-code.js:364`）
- [x] `refactor:` 重组 `app.js` 的 `state` 为 `world` / `pointer` / `dialogs` / `storage` 子对象
- [x] `refactor:` 抽离对话框与函数库管理为独立模块（scripts/dialogs.js）

## P1：架构解耦

- [x] `refactor:` 抽离共享演化核心 `nextCellSet` 到 `life-engine`（避免规则实现分叉）
- [ ] `refactor:` 拆分 `logic-code.js` 为 `parse` / `expand` / `compile` / `safety` 子模块
- [ ] `fix:` `phaseShiftCircuit` 补齐 `signalCells` / `terminalCells` 的相位推进（CODE_REVIEW 第四节第 6 条）

## P2：工程化与代码味道

- [x] `chore:` 补 `LICENSE`（MIT）
- [x] `chore:` 加 ESLint zero-config（pre-commit 钩子暂缓，避免阻塞提交流程；以 `npm run lint` 本地执行）
- [x] `ci:` 加 GitHub Actions 跑 `node --test`
- [x] `fix:` `renderer.js:169` 冗余 `Math.min(size, size)`
- [x] `fix:` `app.js` 撤销清空时恢复 `wasRunning`
- [x] `fix:` 赋值错误诊断信息提升（当前统一报"值只能是 0 或 1"）

## 验收

- [ ] `node --test` 88 用例仍全过
- [ ] `file://` 打开无 JS 错误，核心交互（演化 / 放置 / 导入导出 / 逻辑门生成）正常
- [ ] 写 `docs/重构版测试报告-v0.15.0.md` + `docs/阶段性总结-v0.15.0.md`
- [ ] 打 `git tag v0.15.0`，合并回 `main` 并两端 push

## 依据

来源：本仓库 `CODE_REVIEW.md` 第八节「后续维护建议（按优先级）」与第四节设计级隐患。
