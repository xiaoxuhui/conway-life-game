# 变更日志

本文件记录重要的用户可见变化。版本号遵循语义化版本；发布日期在正式发布时填写。

## [0.16.0] - 2026-09-11

### Added

- 安卓 APK 版本：极简 WebView 外壳 + 内置网页资源，完全离线可用，不申请任何权限；
  包名 `com.xiaoxuhui.conway`，minSdk 24。
- 安卓版的三处导出（世界 / 图案库 / 函数库）改由 JS 桥写入系统下载目录，
  取代 WebView 不支持的 Blob 下载；导入沿用系统文件选择器。
- 安卓版应用图标（自适应图标 + 各密度传统图标）与深色系统栏配色。
- “我的函数”版本化 JSON 导出，以及带完整校验的合并/替换导入。
- “我的图案”全部保存，以及排除内置图案、带完整校验的合并/替换导入。
- 可旋转、翻转放置的真实 90°单脉冲滑翔机反射器，并显式记录 60 代恢复限制。
- 可承载 p30 连续逻辑信号的 p5 90°反射器；四枚连续输入已通过 420 代规则演化验证。
- 电路编译器的完整 D4 坐标变形；嵌套门可选择变形与 0–3 代相位，全部安全元数据同步转换。
- 大型 XOR 与任意嵌套函数的逐连接双 p5 反射后备；仅在全部正常布局失败后启用，并继续对完整电路失败关闭。

## [0.15.0] - 2026-08-28

### Added

- MIT License、ESLint、GitHub Actions 和可复现依赖锁。
- 对话框模块，以及逻辑解析、展开、编译、安全验证四个独立 UMD 模块。
- 共享字符串键细胞集合演化核心和相位元数据回归测试。
- 贡献、安全、行为准则及 GitHub 协作模板。

### Changed

- 项目版本与页面资源缓存参数统一为 v0.15.0。
- CI 在受支持 Node.js 版本上执行语法、lint 和完整测试。

### Fixed

- 函数展开替换中的特殊 `$` 文本处理。
- 清空后撤销恢复运行状态。
- `phaseShiftCircuit` 的输出、终端和连接探针时间轴。
- 重构时遗漏的 `elements.zoomValue` 映射，该缺失会导致 `render()` 在每次调用时抛出异常，进而使放置、清空、随机、运行等全部依赖渲染的按钮失效。

正式发布记录及早期版本细节见 `docs/stage-summary.md` 和 Git 提交历史。
